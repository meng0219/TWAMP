<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.PEGA_latency_merger_tool">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TWAMP_PEGA">
        <activity
            android:name=".tracePage"
            android:label="Traceroute tool" />
        <activity
            android:name=".twampPage"
            android:label="TWAMP tool" />
        <activity
            android:name=".pingPage"
            android:label="Ping tool" />
        <activity
            android:name=".MainActivity"
            android:label="PEGA_latency_merger_tool">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>